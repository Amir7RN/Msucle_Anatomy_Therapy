node_modules/
dist/
.DS_Store
*.local
.env
# Large binary model files — check these in separately
# public/models/*.glb
vite.config.ts.timestamp-*
# Videos/ is intentionally tracked — exercise clips are bundled by Vite
# at build time via new URL() imports from src/. If a specific subfolder
# gets too large, exclude it explicitly here (e.g. Videos/Raw/).
